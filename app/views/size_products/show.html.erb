<p id="notice"><%= notice %></p>

<p>
  <strong>Product:</strong>
  <%= @size_product.product.name %>
</p>

<p>
  <strong>Size:</strong>
  <%= @size_product.size.name %>
</p>

<%= link_to 'Edit', edit_size_product_path(@size_product) %> |
<%= link_to 'Back', size_products_path %>
