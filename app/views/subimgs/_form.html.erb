<%= form_with(model: subimg, local: true) do |form| %>
  <% if subimg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subimg.errors.count, "error") %> prohibited this subimg from being saved:</h2>

      <ul>
      <% subimg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.select :product_id,
      Product.all.order('name').collect{|p| [( p.name), p.id]}
    %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
