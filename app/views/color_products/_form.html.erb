<%= form_with(model: color_product, local: true) do |form| %>
  <% if color_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(color_product.errors.count, "error") %> prohibited this color_product from being saved:</h2>

      <ul>
      <% color_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= form.label :product_id %>
    <%= form.select :product_id,
        Product.all.order(:name).collect{|p| [( p.name), p.id]}
    %>
  </div>

  <div class="field">
    <%= form.label :color_id %>
    <%= form.select :color_id,
        Color.all.order(:name).collect{|c| [( c.name), c.id]}
    %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
